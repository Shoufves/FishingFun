# 智能体记忆库（AGENT_MEMORY）

> 本文件由 AI 助手维护，记录每次会话的任务执行情况，用于跨会话上下文恢复。

## 会话记录

| 日期 | 任务 | 结果 | 备注 |
|------|------|------|------|
| 2026-07-25 | 初始化项目文档 | 完成 | 已提交初始 commit |
| 2026-07-25 | T-001: 脚手架与Canvas初始化 | 已完成，文件创建并自检通过 | 无特殊阻塞 |
| 2026-07-25 | T-002: CSV数据加载器 | 已完成，解析器自检通过 | 无特殊阻塞 |
| 2026-07-25 | T-003: localStorage存档系统 | 已完成，读写与迁移逻辑自检通过 | 无特殊阻塞 |
| 2026-07-25 | T-004: 画面路由系统 | 已完成，标题与地图选择占位屏可正常切换 | 无特殊阻塞 |
| 2026-07-25 | T-005: 像素渲染管线与背景系统 | 已完成，离屏渲染与视差背景自检通过 | 无特殊阻塞 |
| 2026-07-25 | T-006: 音频引擎 | 已完成，合成音效与BGM切换自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-007: 抛竿蓄力条 | 已完成，蓄力条交互与判定自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-008: 等待状态与鱼选择 | 已完成，等待计时与浮漂动画自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-009: 双耐力条搏鱼小游戏 | 已完成，判定逻辑与 UI 自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-010: 鱼生成算法 | 已完成，分布与变异逻辑自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-011: 结果画面 | 已完成，结算 UI 与纪录检查自检通过 | 无特殊阻塞 |
| 2026-07-26 | 数据表内容修正 | 完成，见下方详细记录 | 用户授权后修改4份CSV |
| 2026-07-26 | T-012: 装备属性效果集成 | 已完成，公式集中管理与装备接入自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-017: 装备管理 | 已完成，装备 CRUD 与属性计算自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-013: 饵料消耗系统 | 已完成，消耗逻辑与集成自检通过 | 无特殊阻塞 |
| 2026-07-26 | T-014: 端到端集成测试 | 已完成，测试清单已创建，12 个用例覆盖全流程 | 测试执行需手动验证 |
| 2026-07-26 | T-015: 经济管理器 | 已完成，经验曲线/金币/等级/称号/统计，接入结算写回与自动存档 | 浮点保护：xpForLevel 加 1e-9 |
| 2026-07-26 | T-018: 地图解锁系统 | 已完成，15 张地图加载 + 等级解锁 + 滚动列表 + 新解锁高亮 | 替换原 5 张占位图 |
| 2026-07-26 | T-016: 商店系统 | 已完成，装备/饵料页签、等级/金币校验、禁售状态 | 装备库扩至 20 件并补价格 |
| 2026-07-26 | T-017 补 UI: 装备界面 | 已完成，四槽装备栏 + 背包列表 + 总属性展示 | 渲染与点击区域共用坐标常量 |
| 2026-07-26 | T-019: 图鉴系统 | 已完成，FishDex 管理器 + 分页网格界面（剪影/详情/完成度） | 305 鱼种分页浏览 |
| 2026-07-26 | 入口整合与设置 | 已完成，标题二级菜单 + 地图页商店入口 + SettingsScreen（音量/存档管理） | 存档可导出/删除 |
| 2026-07-26 | 测试体系搭建 | 已完成，49 个单元/冒烟测试全部通过（npm test） | 含模块图完整性测试 |
| 2026-07-26 | T-027~T-030: Phase 6 接口骨架 | 已完成，烹饪/制作/排行/账号 4 个占位类 + SaveManager 数据槽与云同步桩 | task.md 30/30 全部完成 |
| 2026-07-26 | 启动引导冒烟测试 | 已完成，mock DOM/localStorage/Canvas/fetch 完整跑通 bootGame | 52 例测试全通过 |
| 2026-07-26 | 4 项 bug 修复 + T-009.1 复杂键型 | 已完成，见下方详细记录 | 58 例测试全通过 |

### 2026-07-26 Bug 修复与音游优化记录（用户反馈驱动）

**Bug 修复：**
1. **栈深返回异常**：根因=触屏 `touchstart`+合成 `click` 双触发导致重复导航 + RESULT"返回地图"连 pop 两次对栈深敏感。
   - 修复：新增 `ScreenRouter.popTo(type)` 一步到位返回；路由 push/replace 300ms 同类型防抖、pop 300ms 防连按；`main.js` 350ms 触屏合成 click 防护。
2. **竖版界面溢出**：地图描述最长 65 字直接绘制必溢出 → `_truncateText` 按宽度截断加省略号；右侧状态文字上移避免重叠。
   - **音游键起点错位**：键在 `timeLeft>1500ms` 时 `visualProgress<0` 从轨道框**外侧**滑入 → clamp 下界 0，键严格从轨道框右边缘出现；轨道框竖版改占宽 86%（不再贴边）。
3. **饵料耗尽后新手无法钓鱼**：根因=饵料在**抛竿开始**就消耗（Cast Fail 也扣）+ 商店饵料 `minLevel=rarity×3`（红蚯蚓要 Lv.3）。
   - 修复：饵料改为**抛竿判定成功后才消耗**（Cast Fail 不消耗，符合 spec 2.1.2）；无饵料时 WaitSystem 用基础吸引力 40 兜底；商店基础饵 1 级可购（minLevel=rarity）。
4. **咬钩后画面卡顿**：背景每帧全量重绘（渐变+贝塞尔山峦+树）+ 热路径大量 `shadowBlur`。
   - 修复：BackgroundLayer 静态层离屏缓存（每帧仅 drawImage）；浮动伤害/Hit!/判定文字 shadowBlur 收敛；Hit! 抖动改确定性正弦。

**新功能（AGENT_MEMORY 待优化落地）：**
- **T-009.1 音游复杂键型（重点）**：CatchSystem v2.0 — hold 长按（×1.5 伤害，keydown 启动/keyup 结算，含提前松/松晚分级与 keyup 丢失超时保护）、double 双连击（160ms 间隔）、triplet 三连滚奏（120ms）、accel 变速段（4 键间隔 ×0.6）；按 `complexity=min(1,(FP+Rarity)/20)` 概率生成；CatchUI 渲染 hold 长条键（HOLD 标记）。
- **T-009.2 耐力反馈**：鱼表情随耐力变化（平静🐟→生气😠→皱眉😣→翻白眼😵）、大鱼（Rarity≥7/FP≥7）搏鱼边缘红色光晕、低耐力脉冲边框+LOW! 警告。
- **T-011.1 结算增强**：FishGenerator 记录 `caughtAt`，结算画面显示捕获时间。
- **开发者模式**：设置页"⚡ 开发者模式"按钮 → 一键满级（Lv.99）+ 99999 金币并自动存档。

**测试**：58 例全通过（新增 hold 完整/提前松/未按/超时保护、double 连击、复杂键型生成、popTo 栈深验证）。

### 2026-07-26 hold 双判定点重构 + 设置页竖屏修复（用户反馈驱动）

1. **hold 重设计（参考音游）**：由"头部启动+尾部一次性结算"改为**头判+尾判两个判定点，物量只占 1**：
   - 头判：头部到达目标区时的按下精度（Perfect/Great/Good/Miss，窗口同 tap），伤害=基础伤害×精度系数（吃连击暴击），**combo +1（占物量）**；头判 Miss → 整体 Miss。
   - 尾判：尾部到达时松开精度（|偏移|≤150ms→perfect×0.6；≤300ms→good×0.3；过早松>300ms→miss），**不改变 combo**；尾判 Miss 断连击并扣 12% 耐力。
   - 视觉：按住期间长条持续显示——头部亮块锁定目标区（按住点），长条从目标区向尾部收拢；keyup 丢失自动尾判。
   - 常量：`TAIL_DMG_MULT = {perfect:0.6, good:0.3, miss:0}`；移除旧 `HOLD_DAMAGE_MULT`。
2. **设置页竖屏溢出**：音量行 480px 固定宽 + 存档按钮 412px 在窄屏超界 → `_volumeLayout()` 自适应布局（渲染与点击区域共用同一计算）；开发者/存档按钮宽度随窗口压缩。
3. **测试**：63 例全通过（hold 头判/尾判/过早松/头判 miss/超时保护 + 竖屏布局越界断言）。

### 2026-07-26 hold 提前消失的两次修复（用户反馈驱动）

- **第一次（渲染层）**：`_drawTrack` 用 `note.visible`（CatchSystem 400px 虚拟轨道判定）过滤，头判后 offset<-100 判不可见 → 长条提前消失。修复：跳过条件改为仅 `hit||missed`（`_shouldSkipNote`），hold 键可见性基于尾部时间（头部过目标区后仍显示，直到尾部通过 200ms 或尾判完成）。
- **第二次（输入层，最终根因）**：玩家按住空格进行 hold 长按时，浏览器持续派发 keydown（**自动重复**），第二次 `handleInput` 落到普通判定分支，`_applyHit` 把 `holdActive` 的 hold 键打掉（hit=true）→ 长条立即消失。修复（双层防护）：FishingScreen 忽略 `e.repeat`；CatchSystem 中 `holdActive` 的 hold 键在重复 keydown 时直接返回 hold 状态。
- **测试**：65 例全通过（新增"重复 keydown 不打断 hold"回归用例；修复 2 个受生成序列影响的 flaky 测试，连续 3 次运行全绿）。

### 2026-07-26 音游卡顿根治 + hold 卡顿误杀修复（用户反馈驱动，最终根因）

- **卡顿根因**：① 主 Canvas DPR 无上限，高分屏(3x/4x)物理像素达 9~16 倍，全屏 fillRect/shadow/文字开销巨大；② WaterAnimation 每帧 4 波层×80 点 sin+20 光点；③ 热路径大量 shadowBlur（命中动画 16/浮动伤害/判定文字/组合文字）。
- **修复**：DPR 上限 2（像素减 2~4 倍）；水面动画减量+`setQuality` 低档（2 层/6 光点）；CatchUI/FishingScreen 热路径 shadow 全移除（双层文字模拟阴影，视觉等价）；main.js 帧预算监控（连续 15 帧 >40ms 自动降质，恢复后升回）。
- **hold 消失最终根因**：卡顿帧 dt 巨大 → `elapsed` 快速越过 `holdStart+duration+150ms` → 超时保护（keyup 丢失兜底）**误触发** → 长条消失（60fps 下逻辑完全正常，已用诊断脚本验证）。
- **修复**：`CatchNote.lastKeydownAt` 按键活动时间戳 + `handleHoldKeepAlive()`（e.repeat 在 CATCHING 阶段转发保活，不触发普通判定）；超时保护需"尾部已过 **且** 300ms 内无按键活动"才触发——卡顿时玩家持续按住不会被误杀，真正松键且 keyup 丢失仍能兜底。
- **测试**：66 例全通过（新增"hold 卡顿鲁棒：大 dt 帧+保活不误杀"回归）。

### 2026-07-26 参考 osu 的音游性能与判定优化（用户反馈驱动）

- **性能（tap-only 也卡顿的根治）**：
  - `getState()` 每帧 map 创建 ~25 个对象 → 改为 **note/浮动文字视图对象池 + 状态单例复用**，每帧零分配，消除周期性 GC 停顿（osu 避免分配的核心思想）。
  - **轨道静态层离屏预渲染**：背景/目标区/虚线/狂暴覆盖只绘制一次，每帧仅 drawImage（osu 预渲染）。
  - 浮动伤害渲染去掉每帧 filter/slice 数组分配。
- **判定精度（osu 核心：判定用输入事件时间戳，与帧率无关）**：
  - `_resolveJudgeTime`：`handleInput`/`handleHoldRelease` 接收 `e.timeStamp`（与 performance.now 同源），头判/尾判/保活全部基于输入时刻；低帧率下不再因 `_elapsed` 超前/滞后导致"明明按准却 Miss"。
- **hold 显示验证**：新增绘制验证测试（mock 记录 fillRect，断言头判后 hold 长条宽>20px 确实被绘制，排除渲染层静默不画）+ 低帧率头判/尾判精确性测试。
- **测试**：69 例全通过（连续 3 次）。

### 2026-07-26 T-021 音游难度平衡 + 性能二轮（用户反馈驱动）

- **hold 显示已确认修复**（用户反馈"hold 可以显示了"，整个流程可玩）。
- **音游难度平衡（整体拔高+曲线更陡）**，FormulaSheet 调整：
  - 键数 `4+FP×1.2+R×0.4` → `4+FP×1.5+R×0.5`（金枪鱼 17→21，传说 20→24）
  - 间隔 `800-FP×30` → `760-FP×40`（金枪鱼 530→412ms，传说 500→371ms）
  - 速度 `80+FP×6` → `84+FP×8`（金枪鱼 134→174px/s）
  - 鱼耐力 `FP×12+R×6+20` → `FP×14+R×8+24`（金枪鱼 176→214，传说 200→244）
  - 狂暴阶段补充键间隔 ×0.75（实现 spec 狂暴间隔缩短 25%）
  - 难度曲线实测：键数 5→9→13→21→24、间隔 742→659→577→412→371ms
- **性能二轮**（消除"随时可能卡顿"的剩余 GC 峰值）：
  - `_extendNotes` 补充键**复用已处理键**（`_obtainNote` splice+重置），替代每批 new 5 个对象
  - WaterAnimation 渐变缓存（避免每帧 createLinearGradient）
  - FishingScreen 地图名缓存（避免每帧查表）
- **文档同步**：spec.md 2.3.2/2.3.3/2.3.5、test_checklist U-025/031/044 更新；plan.md 锁定未动（差异记录于此）。
- **测试**：69 例全通过（期望值同步更新，连续 3 次）。

### 2026-07-26 装备界面集成鱼饵 + 基础饵兜底（用户需求）

- **装备界面五槽位**：竿/轮/线/钩/**饵**，鱼饵槽显示当前饵料与吸引力。
- **分类筛选**：未选中槽位 → 按顺序显示全部装备（含饵料）；选中某槽位（如鱼竿）→ 只显示该类型，点击列表项**替换**到槽位；再点槽位取消筛选。
- **槽位不允许为空**：移除"卸下"交互（槽位只能被替换），初始装备兜底。
- **基础饵（ID 0）**：恒可装备、数量无限（`getBaitCount(0)=Infinity`）、吸引力 40；`consumeBait` 不消耗数量；真饵耗尽自动回退基础饵；`restoreState` 旧档无饵/耗尽也回基础饵——**任何情况下都能钓鱼**（新手闭环彻底解决）。
- **FishingScreen**：默认基础饵，不再出现"无饵料"提示；B 键循环切换含基础饵。
- **测试**：新增 BaitSystem 基础饵 7 例 + 装备分类筛选断言，**76 例全通过**。

### 2026-07-26 T-021 平衡二轮：整体显著加难（用户反馈"10 秒耗光太简单"）

- **基础伤害**：22.58→15.1（基础装）——强度/21+锋利/12+速比×1.3+拉力/15
- **鱼耐力**：FP×14+R×8+24 → FP×20+R×12+36（金枪鱼 214→312、传说 244→356）
- **玩家耐力**：265→170（基础装）——强度×1.0+刹车×3.0+拉力×0.5+35，miss 容错仅 ~8 次
- **标记间隔**：760-FP×40 → 700-FP×50（金枪鱼 412→260ms、传说 500→260ms）
- **判定窗口收窄**：Great ±60→±50、Good ±100→±80（更高精度要求）
- **效果**：普通玩家(65%命中)打金枪鱼 21 键/260ms/miss 容错 8 → 大概率失败；传说鱼普通玩家必死；高手全 P 5~6s；低难度鱼保持新手友好（鲫鱼 5 键/670ms）
- **文档**：spec.md 2.3.2/2.3.3/2.3.4、test_checklist U-027~032/044 同步
- **测试**：76 例全通过（期望值同步，连续 3 次）

### 2026-07-26 音游偶发卡死修复（用户反馈："键落下但按键无效、一直 miss 不掉血、过一会恢复"）

- **根因**：`_obtainNote`（上轮性能优化）复用已处理键时 `splice` 移除数组元素 → 数组前移 → `_currentNoteIdx` 指向错误的键（远在未来的键）→ 所有按键被判"太早"（`handleInput` 太早分支返回 miss **且不扣血**）→ 键照常落下但判定失效；多次补充错位累积 → 卡 1~3 秒后该键到达才恢复。
- **修复**：移除 splice 复用，改为 `start()` 预生成 **40 个备用键池**（`_spareNotes`），`_extendNotes` 从池取键**只 push 不 splice** → 索引永不破坏，且保持零运行期分配（GC 优化不丢）。
- **测试**：新增"补充键后判定连续性"（40 键打满，验证 idx 前无跳过键、expectedTime 单调），**77 例全通过**。

### 2026-07-26 蓄力条目标区突变 + 移动端触摸滚动（用户反馈）

- **蓄力条绿区开始后突变**：就绪态用 CastingSystem 构造器默认区（perfect 8%），`start()` 经 `calcPerfectZoneWidth` 计算并 `clamp(10,50)` → 8%→10% 突变。修复：构造器接受装备参数，新增 `_applyZone(equipment)` 供构造与 start 共用，就绪态即显示一致的目标区。
- **移动端触摸滚动**：`Screen` 基类新增 `scrollBy(deltaY)` 接口（地图/商店/装备列表实现）；`main.js` touchstart 记录起点 / touchmove 增量驱动 `scrollBy` / touchend 按位移（<8px=点击，≥8px=拖动）分发，音游打键不受影响。
- **测试**：新增"就绪态与开始后目标区一致"回归，**78 例全通过**。

### 2026-07-26 设置页新增导入存档（用户需求）

- `SaveManager.importSave(jsonString)`：JSON 解析 + 关键字段校验 + 写入 localStorage，返回 `{ok, error}`。
- SettingsScreen：存档管理改三按钮（**导入/导出/删除**）；导入弹 DOM 模态（textarea 粘贴，兼容多行格式化 JSON，错误就地提示，成功后自动重载）。
- **测试**：新增 SaveManager 导入 5 例（合法/非法 JSON/缺字段/多行/导出-导入往返），**83 例全通过**。

### 2026-07-26 抛竿目标区缩小 + 触摸系统重做（用户反馈）

- **绿色目标区缩小至约 1/4**：`DEFAULT_PERFECT` 8→2.5%、clamp 下限 10→2.5；光标速度 83→50%/s 配合（Perfect 判定窗口保持约 ±25ms）；高级装备区更宽+光标更慢（装备价值保留）。
- **触摸系统重做（参考现代 App 拖动）**：
  - **修复反向**：手指上滑 → 内容上移（`scrollBy(-dy)`）。
  - **消除点击延迟**：游戏内（音游/蓄力）点击回到 `touchstart` 立即判定；滚动型屏幕（地图/商店/装备）用 `scrollBy` 覆盖判定，拖动时 touchend 不触发点击。
  - **惯性滚动**：松手后按最后速度衰减（0.92/16ms），拖动阈值 6px。
- **测试**：新增完美区宽度断言，**84 例全通过**（连续 3 次）。

### 2026-07-26 四项大改动（用户需求：数值调高/Boss 模式/钓场差异化/饵料感知）

**需求1 数值大幅调高**：鱼血量 `FP×60+R×150+100`（四星 R4 血量 1000 过千、传说 2200）；基础伤害提升至 37（基础装）；玩家耐力 235；战斗时长 5~20 秒。
**需求2 Boss 模式**：3 条 Boss（深渊巨鳗 6000 血/痛击特性/3 秒免疫 Good 冷却 10 秒/3000 金；熔岩鲶 5000 血/耐力上限-20%/免疫 Great/2500 金；极光龙鲤 8000 血/对玩家伤害+100%/仅 Perfect 有效/5000 金）；CatchSystem 支持 Boss 特性与周期技能 + 键型更密集；地图页 ⚔BOSS 入口 → BossSelect（信息卡片+已击败✓）→ BossBattle（复用 CatchUI，胜利金币+已击败标记持久化，可重复挑战）。
**需求3 钓场加成**：难度越高鱼血量 ×(1+(难度-1)×0.12)、体型+、Rarity≥5 选鱼权重提升。
**需求4 饵料感知**：TargetCategory 匹配选鱼权重 ×1.5、饵料体型加成（(R-1)×6%）、钓鱼界面与商店 UI 展示。
**文档**：spec.md（数值+第 11/12 章）、test_checklist、README（游戏系统概览）、task.md 同步。
**测试**：新增 Boss 单测（特性/技能/血量/键密度）+BossData 完整性+渲染冒烟，**93 例全通过**。

### 2026-07-26 设置新增难度/显示模式（用户需求）

- **难度模式**（默认轻松）：轻松判定窗口 Perfect±40/Great±80/Good±120ms，困难=原设计（±25/±50/±80）；CatchSystem `setJudgeMode` 可配置，start 不重置。
- **显示模式**（默认横板）：竖版模式轨道旋转 90°（键从上往下落，目标区在底部）、竖条血从顶部往下扣、条/轨道大小不变；CatchUI 新增 `_renderPortrait`/`_drawVerticalBar`/`_drawPortraitHold`。
- 存档 `settings.difficulty/orientation` 持久化；钓鱼与 Boss 战斗均生效。
- **测试**：新增判定模式与竖版渲染用例，**94 例全通过**（连续 3 次）。

### 2026-07-26 轻松模式回血 + 竖版布局 + 自适应文本 + 音游手感 + 存档文件（用户反馈）

- **轻松模式耐力**：miss 扣血 12%、great/good 不扣、perfect 回血 +4%（Boss 特性只放大受到的伤害，回血不受影响）；困难模式保持原规则。
- **竖版布局**：双耐力条紧贴轨道两侧（消除与音游区域过远）。
- **自适应文本**：Boss 介绍/图鉴详情/装备属性总结按可用宽度截断加省略号，窄屏不重叠/超界。
- **音游手感**：触摸判定传入事件时间戳（`touch.timeStamp` → handleClick → handleInput），连点判定与帧率无关，缓解"吞键"。
- **存档文件**：导出下载 `angler_save_*.json` 文件（替代剪贴板，避免长度截断）；导入支持选择 JSON 文件（FileReader）+ 粘贴文本备选。
- **测试**：轻松模式耐力用例，**95 例全通过**（连续 3 次）。

### 2026-07-27 竖版 4 项渲染 bug 修复 + Boss 复杂键型库（用户反馈）

**竖版渲染修复（用户反馈 bug 1/2/3）：**
1. **hold 长条不显示**：竖版 hold 长条范围计算错误——头判前 `startY=headY`、`endY=max(headY,tailY)=headY` → 高度恒为 0；头判后 `[targetY→tailY]` 反向同样为负高 → **两种状态都不画**（横版方向正确所以正常）。修复：头判前 `[tailY(上)→headY(下)]`、头判后 `[tailY→targetY]`。
2. **伤害数字不显示**：`_renderPortrait` 从未调用浮动伤害渲染（横版有、竖版漏）。新增 `_drawPortraitFloatingDamage`（从鱼条右侧弹出，26px 字号，最多 4 个）。
3. **条/轨道比横板长太多**：竖版 `barH=trackH=h-114` 撑满屏幕（手机 ~730px vs 横板 ~335px）。修复：竖版长度复用**横板分支同一长度公式**（同一窗口下两模式等长），有界且居中。
4. **音符速度**：视觉旅行时间恒定 1500ms；修复前竖版轨道更长 → px/s 更快；修复后两模式长度一致 → 速度一致。

**键表生成重构（用户反馈 bug 5：Boss 后期键"从屏幕中间突然冒出、五个一组"）：**
- **根因**：旧实现键表只生成 5~24 个，战斗靠 `_extendNotes` 每批补充 5 个纯 tap 键，且 `nextTime = elapsed+500` < 视觉窗口 1500ms → 新键出现时已在屏幕中段。
- **修复**：键表按"整场所需命中次数"预生成（`ceil(鱼耐力/(基础伤害×0.75))×1.3`，且 ≥ calcMarkerCount），战斗全程零补充；`_extendNotes` 兜底改为 `nextTime ≥ elapsed + 1500 + 50`（新键从轨道起点进入）。
- 普通鱼键表也变长（如鲤鱼 9→28 键），不再中途冒出。

**Boss 复杂键型库（用户需求：参考太鼓达人等 1K 音游）：**
- `BOSS_PATTERN_LIB`：trill（`xx xx xx` 组内 110ms/组间 240ms）、burst4（`xxxx`）、stream6（`xxxxxx`）、dblStream（`xxxxxx xxxxxx` 组间 320ms）、streamHold（`xxxxxx|-----`）、mixed（`xx x xx x xxxxxx|-----` 12 连打+长按）、stair（变速楼梯 110~240ms）。
- Boss 战（`fish.isBoss`，BossBattleScreen 注入）轮换键型、不连续重复，键型间留呼吸间隔；hold 只允许在键型末尾（单键输入限制）。
- 普通鱼保持 tap/double/triplet/hold/accel 混合。
- **测试**：新增 7 例（键表覆盖整场+全程命中不补充、补充键起点 ≥1500ms、Boss 键型含 hold 与 ≤130ms 连打、键型库结构合法、竖版条/轨道等长、竖版伤害数字、竖版 hold 头判前长条），**102 例全通过**（连续 3 次）。

### 2026-07-27 Boss 伤害下调 + 音符下落速度变慢（用户反馈）

- **Boss 对玩家伤害下调**：深渊巨鳗 `playerDamageMult 1.5→1.2`（痛击 "提高 50%"→"提高 20%"）；熔岩鲶 `playerStaminaMult 0.8→0.9`（"上限降低 20%"→"降低 10%"）；极光龙鲤 `playerDamageMult 2.0→1.5`（"提高 100%"→"提高 50%"）。轻松模式 miss 12%×倍率 → 龙鲤 24%→18%。
- **音符下落速度稍慢**：视觉旅行时间 `NOTE_TRAVEL_VISUAL_MS 1500→1700ms`（约慢 13%）；`PREP_TIME` 改为与视觉旅行时间联动（=1700），保证第一个键仍从轨道起点出现。补充键起点 `+1500+50` 自动变为 `+1700+50`。
- **测试**：3 处硬编码 1500ms 的首键时间改为动态读取 `_notes[0].expectedTime`；新增 2 例（首键到达 ≥ 视觉旅行时间、Boss 伤害倍率锁定 ≤1.5 / 耐力上限 ≥0.9），**104 例全通过**（连续 3 次）。

### 2026-07-26 最终交付状态（可游玩版本）
- **完整闭环可玩**：标题 → 地图选择（15 张，等级解锁）→ 钓鱼（抛竿/等待/搏鱼）→ 结算（金币/经验/纪录写回+升级）→ 商店（装备/饵料）→ 装备更换 → 图鉴收集 → 设置（音量/存档管理），全部自动存档。
- **测试**：`npm test` 52 例全通过（单元 + 模块图 + 启动引导 + 画面 onEnter 冒烟）。
- **启动方式**：`npm run serve`（零依赖 Node 静态服务器）或 `python -m http.server 8080`，浏览器打开 http://localhost:8080 即可游玩。
- **遗留（非阻塞）**：T-020 美术素材、T-021 数值平衡、T-022 水族箱、T-023~T-026 打磨/性能/边界仍为 pending；饵料在抛竿时消耗（spec 建议咬钩时，差异已记录）；WaitSystem 水层/时段修正因子仍为 1.0 占位（BestTime/BestSeason 数据已就绪待 T-021 接入）。

### 2026-07-26 经济闭环与 Phase 3 系统集成记录

- **经济闭环打通**：钓鱼 → 结算（金币/经验写回）→ 升级 → 解锁地图 → 商店消费 → 图鉴收集 → 装备更换，全流程可玩。
- **存档持久化**：`main.js` 新增统一 `window._persist()`，经济/装备/饵料/图鉴任一变更自动写回 localStorage。
- **关键修复**：
  - `xpForLevel` 浮点误差：`floor(100×1.15^1)` 因二进制浮点得 114.99999 → 114，加 `+1e-9` 保护后为 115（与 spec 表一致）。
  - EquipmentScreen 点击区域与渲染纵坐标不一致 → 提取共用 `getRowStartY()`。
  - ShopScreen 无限耐久饵料误跳过金币校验 → 修正。
- **测试**：`node --test --test-isolation=none "tests/**/*.test.js"`，49 例全通过；`npm run serve` 零依赖静态服务器。

---

## 2026-07-26 数据表内容修正记录

### 修改范围
- `table/FishTable.csv` — 308→305 条鱼（合并3组重复物种）
- `table/BaitTable.csv` — 35 行（修正耐久度 + 填充 TargetCategory）
- `table/MapFishSpawn.csv` — 809 行（填充 BestTime/BestSeason/BestWeather、SpawnWeight ÷10、FishId替换、鱼种迁移）
- `table/MapDefinition.csv` — 未修改

### 详细变更

#### FishTable
- **合并 3 组同物异名**：河鲈+赤鲈(48+78)、美洲红点鲑+溪鳟(56+201)、六线鱼+大泷六线鱼(114+115)
- **修正 7 条 MinWeight**：鲤鱼 0.25→0.15、蓝鳍金枪鱼 8.0→4.5、翻车鱼 10.0→3.0、中华鲟 5.0→2.5、锤头双髻鲨 20.0→10.0、锦鲤 0.10→0.05、金鱼 0.005→0.002

#### BaitTable
- **耐久度修正**：蛆虫 12→6、面团 15→3、面包 10→1、豆腐 8→2、米饭 15→3
- **TargetCategory 填充**：11 行空值按生态位补充（小鱼苗→淡水鱼;海水鱼，其余淡水饵→淡水鱼）

#### MapFishSpawn
- **BestTime/BestSeason/BestWeather 全量填充**：809 行×3 列，基于 FishTable.ActiveTime + Description 关键字逐鱼推理
  - BestTime 值：0-24 / 6-18 / 18-6 / 5-7;17-19 / 16-19
  - BestSeason 值：Spring;Summer;Autumn（761 条目，默认）/ Spring;Summer;Autumn;Winter（34 条目，冰钓/耐寒种）/ Spring;Summer（11 条目，北极种）/ Spring;Autumn;Winter（3 条目，夏眠种）
  - BestWeather 值：Sunny;Cloudy;Rainy / Sunny;Cloudy（表层白天种）/ Cloudy;Rainy
- **SpawnWeight 归一化**：全表 ÷10，范围 548~20100 → 55~2010，概率分布不变
- **FishId 替换**：12 行（78→48×5、201→56×3、115→114×4）
- **鱼种迁移**：牙鲆(122)、大菱鲆(123) 从 Map12(深海) → Map9(近海岩礁)

### 关联文档数字同步
- `AGENTS.md` 3.1: 308→305 条鱼
- `spec.md` 1.3: 308→305 种鱼, 行数 309→306
- `spec.md` 6.1: X/308→X/305
- `test_checklist.md` U-001: 308→305 行
- `test_checklist.md` I-004: X/308→X/305
- `plan.md` 2.1: 308→305 种鱼的定义
- `plan.md` Phase 4: 308→305 种鱼

### 风险评估
- **无代码影响**：代码中无逻辑读取 BestTime/BestSeason/BestWeather（WaitSystem._checkBite 的 timeMod=1.0 仍为占位），CSVLoader 兼容
- **正向准备**：BestTime/Season/Weather 数据为未来 MapManager 时间过滤功能提供了基础
- **小心**：未来 T-021 平衡性调整时需注意 SpawnWeight 已是÷10 后的值

## 项目经验教训（历史问题与修复）

### 1. 浏览器缓存导致旧代码执行
- **场景**：T-004 / T-005，修改 `main.js` 后页面仍运行旧代码
- **诊断**：`window._router` 始终 `undefined`，新增日志不出现
- **根因**：浏览器缓存了 ES Module 文件
- **修复**：**必须硬刷新**（Ctrl+F5 / Ctrl+Shift+R），普通刷新不够

### 2. DPR 双重缩放
- **场景**：T-005 Renderer 画面放大 2 倍（Retina 放大 4 倍）
- **根因**：`resizeCanvas()` 设了 `setTransform(dpr,...)`，`_blitToScreen` 再用 `displayW = innerWidth*dpr` 作为目标 → 缩放 dpr^2
- **修复**：blit 前 `save()` + `setTransform(1)`，blit 后 `restore()`

### 3. 变换矩阵未恢复 → 屏幕渲染错位
- **场景**：T-005 UI 只画在画布左上角
- **根因**：`_blitToScreen` 用 `setTransform(1)` 重置后未恢复，后续 `screen.render` 使用 identity 变换而 Canvas 物理尺寸是 `innerWidth*dpr`
- **修复**：`save/restore` 保护上下文状态

### 4. 像素管线小字体模糊
- **场景**：T-005 6-7px 字体拉伸 6x 后糊成一片，15px 字清晰
- **根因**：所有层共用 320×180 离屏绘制
- **修复**：**双管线分离** — 背景/水面走 320×180 像素管线，UI 文字直接在主 Canvas CSS 分辨率绘制

### 5. `_toCamelCase` 不支持 PascalCase
- **场景**：T-002 `FishId` → `fishid`（应为 `fishId`）
- **根因**：只按 `_` 分割，未处理大写字母边界
- **修复**：先 `str.replace(/([a-z0-9])([A-Z])/g, '$1_$2')` 再分割

### 6. BGM 音色诡异
- **场景**：T-006 标题 BGM 听起来沉闷、阴暗
- **根因**：三角波 + 低音区 (C2=65Hz) + drone 铺底
- **修复**：改用高音区正弦波单音旋律（C5=523Hz 起），Minecraft 钟琴风格

### 7. BGM 切换卡顿
- **场景**：T-006 导航到新屏幕时 BGM 从头播放，有静音间隙
- **根因**：`stop→wait 600ms→start` 模型 + MapSelect 路由误触发
- **修复**：交叉淡入淡出（重叠 300ms），BGM 路由只对明确需切歌的屏幕触发

### 8. UI 元素尺寸不一致
- **场景**：T-005 两个"返回"按钮大小不同
- **根因**：FishingScreen 用动态缩放，MapSelectScreen 用固定值
- **修复**：统一使用固定尺寸 90×36

---

## 待优化（后续迭代）

### T-009.1: 搏鱼节奏增强 — 复杂键型 ✅ 已完成（2026-07-26）
- **实现**：CatchSystem v2.0 — hold 长按（×1.5 伤害）、double 双连击（160ms）、triplet 三连滚奏（120ms）、accel 变速段（4 键 ×0.6 间隔）
- **触发**：`complexity = min(1.0, (FP + Rarity) / 20)`，complexity≥0.35 时按概率插入
- **判定**：hold 通过 keydown 启动/keyup 结算，含提前松（×0.5）/松晚（×0.5）/keyup 丢失超时保护（按完整结算）
- **UI**：hold 渲染为青色长条键（HOLD 标记），double/triplet 为紧邻多键

### T-009.2: 耐力反馈增强 ✅ 部分完成（2026-07-26）
- **已实现**：屏幕抖动、闪白、浮动伤害数字、连击显示、低耐力闪烁（原有）
- **本次新增**：鱼面部表情随耐力变化（平静🐟→生气😠→皱眉😣→翻白眼😵）、大鱼（Rarity≥7/FP≥7）搏鱼边缘红色光晕、低耐力脉冲边框 + LOW! 警告
- **未做**：背景色随玩家耐力变化（低优先级）

### T-009.3: 大鱼视觉表现 ⏳ 未完成
- **背景**：中部区域预留给了鱼动画，目前是空的
- **建议方案**：
  - 鱼上钩后显示像素鱼图标
  - 鱼在搏鱼过程中有挣扎动画（左右摇摆、突然冲刺画面拉扯）
  - 稀有度体现为鱼图标特效（发光、色彩、大小）
- **优先级**：低（需等 T-010 FishGenerator 完成）

### T-011.1: 结算画面增强 ✅ 已完成（2026-07-26）
- **已实现**：捕获时间（FishGenerator.caughtAt + 结算"捕获时间"行）；捕获地点（此前已加"钓场"行）

### T-012+T-017: 联合验收 ✅ 已完成（2026-07-26）
- **背景**：T-012 公式接入已通过单元测试（20/20），但需 T-017 装备系统才能可视化验证
- **验收方案**：T-017 完成后，通过开发菜单切换装备档次 → 进钓鱼流程逐一验证：
  - `rod.precision` → 目标区宽窄变化（CastingSystem）✅
  - `reel.gearRatio` → 标记速度与间隔变化（CatchSystem）✅
  - `line.sensitivity` → 等待时间变化（WaitSystem）✅
- **验证方式**：公式中心单测覆盖以上三条（tests/fishing/FormulaSheet.test.js）

---

**下次启动新会话时，AI 应先读取本文件前 10 条记录以恢复上下文。**